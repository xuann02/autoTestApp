appId: com.sima.ceh
---
- launchApp
- tapOn: Daily Challenge
# Màn game + check button Next
- repeat:
    # times: 10
    while:
      visible: "Next"
    commands:
      - runScript: ./randomQuestion.js

      - scrollUntilVisible:
          element:
            id: "mst-Answer.*"
            index: ${output.choice}
          direction: DOWN

      - tapOn:
          id: "mst-Answer.*"
          index: ${output.choice}

      - tapOn: "Next"

# tắt popup Submit
- assertVisible: SUBMIT
- tapOn:
    text: Dismiss
