appId: com.sima.asvab
---
# - runScript: appAndroid.js

- repeat:
    while:
      true: ${output.done != true}
    commands:
      - stopApp
      - clearState

      - launchApp:
          appId: ${output.appId}
          clearState: true

      - runFlow: appModul.yaml

      # - runScript: appAndroid.js
